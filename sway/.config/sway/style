set $font_mono "pango:JetBrains Mono 10"
set $wallpaper "$HOME/Imágenes/wallpapers/sebastian-svenson-d2w-_1LJioQ-unsplash.jpg"

# Main font
font $font_mono

output * bg $wallpaper fill

# Gaps
gaps inner 0

# Hide gaps when there's only one window
smart_gaps on
smart_borders on

# Hide window titles, show thin border
for_window [class="^.*"] border pixel 1

# Use floating by default for some apps
for_window [class="^pavucontrol$"] floating enable
for_window [app_id="^org.pulseaudio.pavucontrol$"] floating enable
for_window [class="^gnome-calculator$"] floating enable
for_window [class="^gnome-screenshot$"] floating enable
for_window [class="^Blueman-manager$"] floating enable
for_window [app_id="^blueman-manager$"] floating enable
for_window [class="^keepassxc$"] floating enable
for_window [app_id="^org.keepassxc.KeePassXC$"] floating enable

bar {
    position top

    # Workspaces, battery life, time and tray icons
    status_command while (echo "BAT $(cat /sys/class/power_supply/BAT0/capacity)% \| $(date +'%Y-%m-%d %X')"); do sleep 1; done

    colors {
        statusline #ffffff
        background #323232
        inactive_workspace #32323200 #32323200 #5c5c5c
    }
}

# vim: set ft=swayconfig:
